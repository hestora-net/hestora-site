# Analytics/credential config files. Never in this repo -- listed here so an
# accidental `git add .` cannot commit one.
.analytics-config.json
*.analytics-config.json

# Any other secret-bearing file.
.env
.env.*
!.env.example

# macOS / Dropbox noise.
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Icon?

# Editor backups.
*~
*.swp
*.swo

# Distribution archives -- the repo IS the distribution now.
*.zip

# Node, if a build step is ever added.
node_modules/
*.log
